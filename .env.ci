# Dummy values for CI type-checking and build.
# SvelteKit $env/static/* requires all variables to exist at build time.
# When adding a new $env/static/private import, add the variable here too.

PUBLIC_SUPABASE_URL=https://example.supabase.co
PUBLIC_SUPABASE_ANON_KEY=dummy
SUPABASE_SERVICE_ROLE_KEY=dummy
ADMIN_PASSWORD=dummy
ADMIN_SESSION_SECRET=dummy
SKIP_LOG_IPS=
RESEND_API_KEY=dummy
