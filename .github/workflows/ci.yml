name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run check
        continue-on-error: true

      - name: Build
        run: npm run build
        env:
          PUBLIC_SUPABASE_URL: https://rilwtpluofguyjpzdezi.supabase.co
          PUBLIC_SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJpbHd0cGx1b2ZndXlqcHpkZXppIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzE0MzIwNDgsImV4cCI6MjA4NzAwODA0OH0.xHnbz1Pz1KTIy6drBsSXMDdNaqumRJPy8GYuyOQtUV4
